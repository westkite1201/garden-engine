{
  "name": "garden-engine",
  "version": "0.1.0",
  "description": "1년간의 커밋이 살아 있는 정원으로 피어납니다",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "generate": "npm run build && npm run start"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
